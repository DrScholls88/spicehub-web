// Leftover scratch file from a shadow-verification pass — safe to delete.
import { describe, it, expect } from 'vitest';

describe('_zzdebug (safe to delete this file)', () => {
  it('noop', () => {
    expect(true).toBe(true);
  });
});
