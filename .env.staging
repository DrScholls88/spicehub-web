# SpiceHub staging environment
# Enable Gemini structured JSON output (test against live key before enabling in prod)
VITE_GEMINI_RESPONSE_SCHEMA=true
VITE_ASR_ENDPOINT=/api/transcribe
NODE_ENV=staging
